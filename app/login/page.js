import LoginPage from "./LoginPage";

export const metadata = {
  title: "Login Page",
  description: "Login Page",
};
const page = () => {
  return (
    <div>
      <LoginPage />
    </div>
  );
};

export default page;
